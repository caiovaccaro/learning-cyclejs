- This exercise have 5 steps
- Each step have its own branch `feature/{number}`
- Each step have its own answer-branch `feature/{number}-a`
- Go from 1 to 5 and check `a` branches if you have doubts

`npm i && npm start`
